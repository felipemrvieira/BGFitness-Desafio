<%= form_with(model: competitor, local: true) do |form| %>
  <% if competitor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(competitor.errors.count, "error") %> prohibited this competitor from being saved:</h2>

      <ul>
      <% competitor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="col-md-5">
  <div class="panel panel-default">
    <div class="panel-heading">
        Competidor
    </div>
    <div class="panel-body">
  <div class="field form-group">
    <%= form.label :name, 'Nome' %>
    <%= form.text_field :name, id: :competitor_name, class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :email %>
    <%= form.text_field :email, id: :competitor_email , class: "form-control" %>
  </div>

  <div class="field form-group">
    <%= form.label :team_id, 'Equipe' %>
    <%= collection_select(:competitor, :team_id, @team_options, :id, :team_gym , class: "form-control" )%>
  </div>

  <div class="actions form-group">
    <%= form.submit 'Salvar', class: "btn btn-outline btn-success" %>
  </div>

  </div>
</div>

<% end %>
